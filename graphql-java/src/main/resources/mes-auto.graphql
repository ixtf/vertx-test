type Query {
    operators(first: Int=0,pageSize:Int=50): OperatorQuery
    hello: String
}

type OperatorQuery {
    first: Int
    pageSize: Int
    count: Int
    operators: [Operator]
}

type Operator {
    id: ID!
    name: String
    hrId: String
    admin: Boolean
}
